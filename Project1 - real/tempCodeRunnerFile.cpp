for (int i = 0; i < 9; ++i) {
                    //     symbol_find = current_line.find("difficulty: ");
                    //     game_array[i] = current_line.at(symbol_find+i);
                    // }